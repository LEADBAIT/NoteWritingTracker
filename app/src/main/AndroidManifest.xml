<?xml version="1.0" encoding="utf-8"?>
<!-- Place at: NoteWritingTracker/app/src/main/AndroidManifest.xml -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <!--
            Single AppWidgetProvider that handles both 2x2 and 4x2 sizes.
            The widget switches layouts automatically when the user resizes it
            on the home screen (handled in onAppWidgetOptionsChanged).

            The LOG_TODAY action is registered here so the widget can receive
            its own tap broadcasts without needing a separate Service or Activity.
        -->
        <receiver
            android:name=".NotesStreakWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.leadbait.notewritingtracker.LOG_TODAY" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/notes_streak_widget_info" />
        </receiver>

    </application>

</manifest>
